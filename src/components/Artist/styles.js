export default ({ spacing }) => ({
  imageContainer: {
    borderRadius: spacing.unit * 25,
    overflow: 'hidden',
  },

  image: {
    width: spacing.unit * 50,
    height: spacing.unit * 50,
  },
});
